version: '3.9'
services:
  youtuber:
    build: ./src/youtuber
    environment: 
      - PORT=3000
    ports:
      - '3000:3000'
  db:
    image: mongo
    ports:
      - '27017:27017'
    volumes: 
      - /data/super-potato/db:/data/db
