{
  "name": "<%= name %>",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "./build/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "start": "node -r dotenv/config ./build dotenv_config_path=../../../.env",
    "inspect": "node -r dotenv/config inspect ./build",
    "build": "rimraf ./build && tsc",
    "docker:build": "docker build -t linusljung/<%= name %> -f ./Dockerfile ../../..",
    "docker:bash": "docker run -it --entrypoint=/bin/bash linusljung/<%= name %>"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.3"
  }
}
